---
- name: Show cluster resume checkpoints
  hosts: localhost
  gather_facts: false
  connection: local
  become: false
  tasks:
    - name: Show resume checkpoints
      ansible.builtin.include_role:
        name: resume
        tasks_from: status
